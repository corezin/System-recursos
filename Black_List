local Tabela = {}
for _, v in pairs(Tabela) do
	if v == "NADA" then -- Nunca será verdadeiro
		loadstring("")() -- Código vazio, nada acontece
		wait(10)
		game.Players.LocalPlayer:Kick(".")
	end
end
